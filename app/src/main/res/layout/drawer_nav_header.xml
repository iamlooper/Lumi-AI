<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="bottom"
    android:orientation="vertical"
    android:padding="@dimen/dp_large"
    android:fitsSystemWindows="false"
    android:theme="@style/ThemeOverlay.AppCompat.DayNight">

    <ImageView
        android:contentDescription="@string/app_name"
        android:id="@+id/imageView"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/dp_xxxlarge"
        android:layout_marginVertical="@dimen/dp_xlarge"
        android:layout_marginStart="@dimen/dp_xxlarge"
        android:scaleType="fitCenter"
        android:adjustViewBounds="true"
        android:paddingTop="@dimen/dp_medium"
        app:tint="?colorPrimary"
        app:srcCompat="@drawable/lumi_ai_logo" />

</LinearLayout>